[package]
name    = "tsoracle-fuzz"
version = "0.0.0"
edition = "2024"
publish = false

# Empty [workspace] table makes this its own workspace root and prevents
# `cargo build --workspace` from the parent repo discovering these targets,
# which is required because the harnesses use #![no_main] and only build
# on the pinned nightly toolchain.
[workspace]

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys            = "0.4"
# `default-features = false, features = ["use-std"]` matches the parent
# workspace pin in `../Cargo.toml`; the fuzz crate is its own workspace root,
# so workspace deps aren't inherited and the same flags must be repeated here.
postcard                 = { version = "1", default-features = false, features = ["use-std"] }
prost                    = "0.14"
# `default-features = false` opts out of `rocksdb-log-store` (toolkit) and
# `rocksdb-snapshot-store` (driver-openraft), which both pull in
# `librocksdb-sys` → `bindgen` → `clang-sys`. The fuzz CI runner does not
# have clang installed, and the fuzz harnesses only exercise pure postcard
# decoders on serde-derived types, so the rocksdb backends are not needed.
# `tsoracle_openraft_toolkit::codec` (the only toolkit surface the harnesses touch)
# is unconditional and lives outside the feature-gated `log_store` module.
tsoracle-openraft-toolkit         = { path = "../crates/tsoracle-openraft-toolkit", default-features = false }
tsoracle-driver-file     = { path = "../crates/tsoracle-driver-file" }
tsoracle-driver-openraft = { path = "../crates/tsoracle-driver-openraft", default-features = false }
# `default-features = false` drops the `rocksdb-storage` feature on both paxos
# crates, keeping `librocksdb-sys` → `bindgen` → `clang-sys` out of the fuzz
# build (the runner has no clang). The harnesses only touch pure postcard
# decoders: `HighWaterCommand`/`HighWaterSnapshot` (driver) and the
# `storage::meta` ballot decoder (toolkit), all of which live outside the
# feature-gated rocksdb `Storage` impl.
tsoracle-driver-paxos    = { path = "../crates/tsoracle-driver-paxos", default-features = false }
tsoracle-paxos-toolkit   = { path = "../crates/tsoracle-paxos-toolkit", default-features = false }
tsoracle-proto           = { path = "../crates/tsoracle-proto" }

[[bin]]
name              = "record_decode"
path              = "fuzz_targets/record_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "record_roundtrip"
path              = "fuzz_targets/record_roundtrip.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "dense_record_decode"
path              = "fuzz_targets/dense_record_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "dense_record_roundtrip"
path              = "fuzz_targets/dense_record_roundtrip.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "proto_get_ts_request_decode"
path              = "fuzz_targets/proto_get_ts_request_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "proto_get_ts_response_decode"
path              = "fuzz_targets/proto_get_ts_response_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "proto_leader_hint_decode"
path              = "fuzz_targets/proto_leader_hint_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "log_entry_decode"
path              = "fuzz_targets/log_entry_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "snapshot_payload_decode"
path              = "fuzz_targets/snapshot_payload_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "toolkit_codec_decode"
path              = "fuzz_targets/toolkit_codec_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "openraft_entry_decode"
path              = "fuzz_targets/openraft_entry_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "openraft_meta_decode"
path              = "fuzz_targets/openraft_meta_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "openraft_dense_command_decode"
path              = "fuzz_targets/openraft_dense_command_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "codec_roundtrip"
path              = "fuzz_targets/codec_roundtrip.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "paxos_log_entry_decode"
path              = "fuzz_targets/paxos_log_entry_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "paxos_snapshot_decode"
path              = "fuzz_targets/paxos_snapshot_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "paxos_meta_ballot_decode"
path              = "fuzz_targets/paxos_meta_ballot_decode.rs"
test              = false
doc               = false
bench             = false
doctest           = false

[[bin]]
name              = "paxos_codec_roundtrip"
path              = "fuzz_targets/paxos_codec_roundtrip.rs"
test              = false
doc               = false
bench             = false
doctest           = false
