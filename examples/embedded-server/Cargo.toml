[package]
name                   = "example-embedded-server"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
description            = "tsoracle example: embed FileDriver in your own binary with graceful shutdown"
publish                = false

[dependencies]
tsoracle-server       = { path = "../../crates/tsoracle-server", version = "0.2.5" }
tsoracle-driver-file = { path = "../../crates/tsoracle-driver-file", version = "0.1.7" }
tokio                 = { workspace = true, features = ["signal"] }
tracing               = { workspace = true }
tracing-subscriber    = { workspace = true }
anyhow                = { workspace = true }
