// Empty library so the fuzz crate's manifest has at least one target.
// Fuzz targets are declared as [[bin]] entries in Cargo.toml and live
// under fuzz_targets/. They are added by subsequent tasks.
