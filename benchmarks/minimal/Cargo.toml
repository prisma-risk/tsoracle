[package]
name = "bench-minimal"
description = "tsoracle overhead benchmark: end-to-end through tonic, in-memory ConsensusDriver. Not a published crate."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
homepage.workspace     = true
readme.workspace       = true
publish = false

[features]
default      = []
tokio-console = ["dep:console-subscriber"]
flamegraph   = ["dep:tracing-flame"]

[dependencies]
anyhow             = { workspace = true }
clap               = { workspace = true, features = ["derive"] }
hdrhistogram       = { workspace = true }
humantime          = { workspace = true }
serde_json         = { workspace = true }
tokio              = { workspace = true, features = ["rt-multi-thread", "macros", "sync", "time", "net"] }
tonic              = { workspace = true }
tracing            = { workspace = true }
tracing-subscriber = { workspace = true }

tsoracle-server    = { path = "../../crates/tsoracle-server", version = "0.2.7", features = ["test-fakes"] }
tsoracle-client    = { path = "../../crates/tsoracle-client", version = "0.2.5" }
tsoracle-core      = { path = "../../crates/tsoracle-core", version = "0.2.4" }
tsoracle-consensus = { path = "../../crates/tsoracle-consensus", version = "0.1.9" }

console-subscriber = { version = "0.5", optional = true }
tracing-flame      = { version = "0.2", optional = true }

[[bin]]
name = "bench"
path = "src/bin/bench.rs"
